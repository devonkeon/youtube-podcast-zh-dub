# HANDOFF — 致 Claude Code（2026-08-02，Kimi 留）

你做到 M3 首轮（`e5e9116`）交接给我。这是之后发生的一切，按你的账本格式写。

## 先说核实结论

你的交接信息我逐条独立复核过：**绝大部分属实**（仓库、素材、M3 全部统计数据、跨说话人句子）。两处小出入，不影
响结论但说明你的数字需要重算：

1. 轮次时长你写 min/med/max = 6.65/46.70/70.97s，按 `subtitle list` 重算是 **6.65/47.35/76.26s**；
2. 你说"4 个轮次边界"，实际是 **4 个轮次、3 个边界**（7.34 / 40.69 / 103.11s）。

另外一个预案失效：你说"看 4 张画面帧就完事"——**这条素材是固定分屏双人同框 + ISS B-roll，静帧看不出谁在
说**。我改用「00:22 人名字幕条锚点（Dan Huot）+ ASR 阶段 3 处 ⏹ 标记与边界逐一对齐 + 双提案 diff」的组合
完成了确认。你担心的跨说话人句子（`comes to down to ⏹ gravity`）在 `subtitle list` 里**本来就是两个 group**
（g8.42=s2 / wmsah7v2h-52=s1，中文译文也在边界断开），group 粒度够用，不用词级二次切分。

## 节点推进（你走的时候 M3 首轮 → 现在 M7+ 完成）

| 节点 | 状态 | 关键证据（都在 docs/BAOCUT_NOTES.md 对应小节） |
|---|---|---|
| M3 说话人验证 | ✅ 过门 | 双提案 diff 0/101 cue；⏹ 三方对齐；人名字幕锚点。s1=Dan Huot，s2=Gary Jordan |
| LLM 提速（你挂的未验证假设 #5） | ✅ 已解 | GUI 能自跑但不接 CLI 任务（探针实验 p3 证实）；最终解是自写 worker-bot，见下 |
| M4 批量 TTS | ✅ 完成 | edge-tts conc 8 零失败；病态组已兜底；rate+15%+去静音 |
| M5 时长适配 | ✅ 完成 | 结构性零漂移；p2 仅 1 单元 >1.5x |
| M6 封装+QC | ✅ 完成 | `output/p2_dubbed.mp4` 双音轨双字幕 + 3 样片 + qc_report.json |
| M7 长播客 | ✅ 完成 ×2 | p5：20 分钟 3 人 ≈25 分钟；p6：**62 分钟 4 人 ≈52 分钟（0.84× 实时）** |

## 新增的两块基础设施（你走的时候代码 0 行）

1. **`worker/llm_worker.py`** — 用 DeepSeek API（opencode 网关）自动驱动 BaoCut task 队列，lint 拒绝自动带
   problems 反馈重试。p6 实测：62 分钟素材 LLM 阶段 25 分钟、158+ 次调用。**你说的"20 分钟播客按小时估"
   已不成立**，但 GUI 自跑路径（架构修正 v2 第 3 条）也验证过属实，可作备用。
2. **`dub/build_dub.py`** — 配音层。四级 TTS 引擎链 **mimo → moss → EdgeSpeak 本地 → edge-tts**，熔断 +
   时长护栏 + 断点续跑。克隆音色（mimo/moss/EdgeSpeak）和通用音色（edge-tts）都出过样片。

## 推翻/修正了你文档里的几条

- **"整批合成后切开，禁止逐句合成"（架构修正 v2 第 1 条）**：对克隆 TTS 成立，但 edge-tts 是确定性云音色无
  漂移，逐句合成+并发才是业界共识且可断点续跑。已改，实测数据在 M4 小节。
- **`cline-pass/deepseek-v4-flash` 的身份**：它不是 LLM，是语音识别目录里的云端 ASR 条目。
- **repunct 质量问题**：p6 规模下依然存在但不阻塞；align lint 拒绝率高（112/79 on p5），靠 bot 反馈循环
  收敛，代价是 token 浪费。

## 留给你/团队的待办（按优先级）

1. **人耳验收**（唯一没闭环的验收项）：`output/samples/` 8 段样片，特别是克隆版 `p2_clone_*.mp4`。
2. **>1.5x 变速句（约 6%）治本**：LLM 重译缩短，方案在 docs/SPEED_RESEARCH.md 第 3 节。
3. align 换更强模型（flash 关推理后一次通过率偏低，retry 风暴）。
4. worker 长任务 `--max-rounds` 默认 200 不够（p6 跑满退出 2 次），建议默认 400+ 或循环补射。
5. p2_clone 的 11 个 >1.5x 单元：克隆音色像不像决定要不要救。

接手入口不变：`STATE.md`（已更新到 M7+）。所有实测原始输出在 `docs/BAOCUT_NOTES.md`。
